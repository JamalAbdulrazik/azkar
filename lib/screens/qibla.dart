import 'package:flutter/material.dart';

class DirectionOfPrayer extends StatelessWidget {
  const DirectionOfPrayer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
